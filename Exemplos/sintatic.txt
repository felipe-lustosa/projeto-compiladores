program sampleProgram;

var i, j, k: integer;
var f, g, h: real;
var arr : array[1 .. 15] of integer;
var flag, test: boolean;

begin
    if (i > j) then
        begin
            arr[1] := f;
            flag := i > k;
        end
    else
        begin
            i := j;
            f := g;
            j := f;
            g := k;
            h := arr[5];
            arr[10] := h;
            while (arr[5] < (f + g + h))
            do
            arr[5] := i;
        end
    ;
end.